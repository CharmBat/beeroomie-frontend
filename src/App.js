import React from 'react';
import { Button } from 'antd';

const App = () => {
    return (
        <div style={{ textAlign: 'center', marginTop: '50px' }}>
            <h1>Welcome to BeeRoomie!</h1>
            <Button type="primary">Get Started</Button>
        </div>
    );
};

export default App;
